# Family WeChat Mini Program

The mini program presents device status and fall events from the care API. Import this directory in WeChat DevTools with a test account. Before testing, replace the placeholder API host in `app.js` with a host you control. Use HTTPS and valid domain registration for a deployed mini program. Local-only `project.private.config.json` and real app secrets are intentionally excluded.
